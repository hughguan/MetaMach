.DS_Store
*.swp
*.swo
*~
docs/bak/
spike/

# M1: build artifacts & local runtime state
# (Check-in Gate #4 - never commit sockets, PIDs, or secrets)
janus/target/
/bin/
metamach_backup_*.sql
*.sock
*.pid
.db_password

# 0.3.0: native PG data dir (never commit host PG data)
~/.metamach/db/

# 0.3.0: SQLite fallback ring buffer (degraded mode)
fallback.db
