# wf_tester_validate — TESTER runs tests and produces test report

[workflow]
name = "wf_tester_validate"
description = "TESTER runs tests and produces test report"

[[steps]]
name = "execute"
agent = "architect"
command = "echo ok"
